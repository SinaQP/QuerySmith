# QuerySmith

QuerySmith is a Python library foundation for converting natural language requests into safe SQL Server `SELECT` queries by combining database schema metadata, guardrails, and language model orchestration. It is not ready yet: this scaffold only includes configuration loading, SQLAlchemy connection helpers, and schema data models; introspection, LLM calls, query validation, and the pipeline are intentionally unimplemented.
